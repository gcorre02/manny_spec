# Candidate Training (currently empty)

Reserved for future candidate training materials. Currently empty; see `canonical/high_level/runtime_protocols/` and `canonical/high_level/unified_manifold/` for existing references.
