# Archived training specs (jazz series)

These files are retained for reference but are not canonical:
- `jazz_training_high_level.md`

See `implementation/v1.1/post_milestones_runtime_spec.md` for the current runtime spec (moved from the candidate folder).
See `canonical/high_level/jazz_protocol.md` for the canonicalized protocol reference.
